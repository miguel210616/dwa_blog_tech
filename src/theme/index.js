import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  // colores
  // fuentes
  // espacios
  // sombres
  palette: {
    primary: {
      light: "#31910A",
      main: "#34495E",
      dark: "#31910A",
    },
    secondary: {
      main: "#31910A",
    },
  },
});

export default theme;